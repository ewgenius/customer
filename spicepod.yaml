version: v1beta1
kind: Spicepod
name: tpch
datasets:
  - from: s3://spiceai-demo-datasets/tpch/customer/
    name: customer
    params:
      file_format: parquet
    acceleration:
      enabled: false
